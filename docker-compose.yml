# Запуск в контейнере (необязательно — проект работает и просто через ./run.sh).
services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data      # база SQLite сохраняется на хосте
